import 'package:flutter/material.dart';
class HouseListingpage extends StatelessWidget {
  const HouseListingpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(

      ),
    );
  }
}
